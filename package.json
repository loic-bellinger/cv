{
  "name": "cv",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "pnpm run check && astro check && astro build",
    "preview": "astro preview",
    "check": "pnpm exec biome check",
    "check:fix": "pnpm exec biome check --write --staged",
    "astro": "astro",
    "prepare": "husky",
    "preinstall": "npx only-allow pnpm"
  },
  "dependencies": {
    "astro": "^5.16.9"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "@biomejs/biome": "^2.3.11",
    "husky": "^9.1.7"
  }
}
